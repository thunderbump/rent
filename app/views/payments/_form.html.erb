<%= form_for(@payment) do |f| %>
  <% if @payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@payment.errors.count, "error") %> prohibited this payment from being saved:</h2>

      <ul>
      <% @payment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :source %>
    <%= f.collection_select :source_id, @users, :id, :first_name %>
  </div>
  <div class="field">
    <%= f.label :destination %>
    <%= f.collection_select :destination_id, @users, :id, :first_name %>
  </div>
  <%= f.hidden_field :creator_id, :value => current_user.id %>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
